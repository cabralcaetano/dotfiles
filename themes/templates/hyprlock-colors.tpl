# Gerado por theme-set.sh — NÃO editar à mão, sobrescrito a cada troca de tema.
# Fonte: themes/<tema>/colors.toml — variáveis hyprlang consumidas por hyprlock.conf
$bg_color = rgba({{ background_strip }}ee)
$fg_color = rgb({{ foreground_strip }})
$accent_color = rgba({{ accent_strip }}ff)
$accent_faint = rgba({{ accent_strip }}33)
$error_color = rgba({{ error_strip }}ff)
