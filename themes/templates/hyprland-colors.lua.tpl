-- Gerado por theme-set.sh — NÃO editar à mão, sobrescrito a cada troca de tema.
-- Fonte: themes/<tema>/colors.toml
return {
    active_border   = "rgba({{ accent_strip }}ff)",
    inactive_border = "rgba({{ background_strip }}ff)",
    border_size     = {{ border_size }},
    gaps_in         = {{ gaps_in }},
    gaps_out        = {{ gaps_out }},
    rounding        = {{ window_rounding }},
    rounding_power  = {{ rounding_power }},
}
