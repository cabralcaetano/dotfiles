-- Gerado por theme-set.sh — NÃO editar à mão, sobrescrito a cada troca de tema.
-- Fonte: themes/<tema>/colors.toml (campo accent / background)
return {
    active_border   = "rgba(a0a0a0ff)",
    inactive_border = "rgba(1d1d20ff)",
}
