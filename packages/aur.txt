# Pacotes AUR — precisam de um AUR helper (yay)
# Manifesto completo de `pacman -Qqem`, menos os pacotes -debug (só símbolos de
# depuração, não são necessários em runtime).
# Instalar: yay -S --needed $(grep -v '^#' packages/aur.txt | grep -v '^$')
# Conferir drift:
#   comm -23 <(pacman -Qqem | sort) \
#            <(sed '/^[[:space:]]*#/d;/^[[:space:]]*$/d' packages/aur.txt | sort -u)
# Atualizado: 2026-09-10 · Arch Linux

# --- AUR helper (instalar primeiro, manualmente) ---
# git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si
yay

# --- Compositor / Wayland ---
# waybar-git: fix do clique em hyprland/workspaces com dispatcher Lua do
# Hyprland (Waybar#5008/#5013), sem release estável ainda — conflita com
# `waybar` do repo oficial, sudo pacman -R waybar antes se necessário.
waybar-git
wlogout

# --- Apps ---
beekeeper-studio-bin
brave-bin
overskride-bin
vesktop
visual-studio-code-bin
# discord e spotify-launcher saíram daqui: estão nos repos oficiais
# (packages/pacman.txt)

# --- iOS / Android ---
# iloader-bin: sideload de .ipa no iPhone via USB
iloader-bin
# waydroid-script-git: instala GApps/libhoudini na imagem do waydroid
waydroid-script-git

# --- Escritório ---
# dicionário pt-BR para o LibreOffice/hunspell (não existe nos repos oficiais)
hunspell-pt-br

# --- Rede ---
# NextDNS: DoH resolver + filtro por dispositivo; ver nextdns/nextdns.md
nextdns-bin

# --- Fontes ---
# emojis coloridos da Apple (renderização melhor que noto-fonts-emoji no Zen)
ttf-apple-emoji

# --- Diversos ---
neofetch
