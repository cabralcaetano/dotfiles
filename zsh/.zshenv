# ~/.zshenv — PATH base, lido por TODO shell zsh (inclusive não-interativo:
# scripts, systemd --user, execs de editor). O ~/.zshrc só roda em shell
# interativo, então o que precisa existir sempre mora aqui.
export PATH="$HOME/.bun/bin:$PATH"
